using UnityEngine;

public class scr_ui_button_manager : MonoBehaviour {
    private void StarGame(string firstLevel) {
        print("There are no levels yet, so this is just a placeholder");
    }
    private void EndGame(string firstLevel) {
        Application.Quit(0);
    }
}